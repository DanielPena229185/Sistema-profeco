import { DineroFormatoPipe } from './dinero-formato.pipe';

describe('DineroFormatoPipe', () => {
  it('create an instance', () => {
    const pipe = new DineroFormatoPipe();
    expect(pipe).toBeTruthy();
  });
});
